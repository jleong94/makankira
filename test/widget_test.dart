import 'package:flutter_test/flutter_test.dart';

// Placeholder so `flutter test` is green; widget/integration tests come with the
// screens that have testable logic.
void main() {
  test('sanity', () {
    expect(1 + 1, 2);
  });
}
