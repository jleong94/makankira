# Normalize line endings: LF in the repo (and working tree) for cross-platform CI.
* text=auto eol=lf

# Windows batch files must stay CRLF.
*.bat text eol=crlf

# Treat common binaries as binary (no EOL conversion).
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.xlsx binary
